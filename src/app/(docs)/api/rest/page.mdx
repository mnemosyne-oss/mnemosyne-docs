export const metadata = {
  title: 'REST API — Mnemosyne HTTP Endpoints Reference',
  description: 'REST API reference for Mnemosyne legacy FastAPI server: HTTP endpoints for memory CRUD, search, session context, and consolidation. Includes request formats and examples.',
  openGraph: {
    images: ['/og-image.jpg?v=1'],
  },
  twitter: {
    card: 'summary_large_image',
  },
};

# REST API

Legacy FastAPI HTTP interface for remote access and service integration.

<Callout type="warning" title="Legacy Interface">
The REST API server is a legacy FastAPI application embedded in `mnemosyne/cli.py`. It is not part of the BEAM architecture. For new integrations, prefer the Python SDK or Hermes plugin.
</Callout>

## Starting the Server

<CodeBlock code={`python -m mnemosyne \
    --host 0.0.0.0 \
    --port 8090 \
    --db-path ./memory.db
`} language="bash" />

Or via Docker:

<CodeBlock code={`docker run -p 8090:8090 \
    -v $(pwd)/memory.db:/data/memory.db \
    mnemosyne/server:latest
`} language="bash" />

## Endpoints

### Status

<CodeBlock code={`GET /

# Response:
{
  "status": "ok",
  "version": "3.1.0",
  "working_count": 42,
  "episodic_count": 128,
  "semantic_count": 64
}
`} language="bash" />

### Store Memory

<CodeBlock code={`POST /store
Content-Type: application/json

{
  "content": "User prefers dark mode.",
  "tags": ["preferences", "ui"],
  "importance": 0.8,
  "source": "conversation"
}

# Response:
{
  "id": "mem_abc123",
  "status": "stored"
}
`} language="bash" />

### Recall (Search)

<CodeBlock code={`GET /recall?query=user+preferences&top_k=5

# Response:
{
  "results": [
    {
      "id": "mem_abc123",
      "content": "User prefers dark mode.",
      "score": 0.95,
      "tags": ["preferences", "ui"]
    }
  ],
  "total": 1
}
`} language="bash" />

### Session Context

<CodeBlock code={`GET /session/{session_id}

# Response:
{
  "session_id": "sess_123",
  "memories": [
    {
      "id": "mem_abc123",
      "content": "User prefers dark mode.",
      "tags": ["preferences", "ui"],
      "importance": 0.8,
      "created_at": "2026-04-25T14:30:00Z"
    }
  ],
  "total": 1
}
`} language="bash" />

### Statistics

<CodeBlock code={`GET /stats

# Response:
{
  "working_count": 42,
  "episodic_count": 128,
  "semantic_count": 64,
  "db_size_mb": 12.5,
  "last_sleep_at": "2026-04-25T13:00:00Z"
}
`} language="bash" />

### Forget (Delete Memory)

<CodeBlock code={`DELETE /forget/{memory_id}

# Response:
{
  "status": "forgotten",
  "id": "mem_abc123"
}
`} language="bash" />

### Update Memory

<CodeBlock code={`PUT /update/{memory_id}
Content-Type: application/json

{
  "content": "User prefers dark mode and large fonts.",
  "importance": 0.9
}
`} language="bash" />

### Consolidate

<CodeBlock code={`POST /consolidate

# Response:
{
  "status": "consolidated",
  "consolidated_count": 15,
  "evicted_count": 3
}
`} language="bash" />

## Error Responses

| Status | Code | Description |
|--------|------|-------------|
| 400 | `INVALID_REQUEST` | Malformed request body |
| 404 | `NOT_FOUND` | Memory not found |
| 500 | `INTERNAL_ERROR` | Server error |

## OpenAPI Spec

The server exposes OpenAPI documentation at:

- Swagger UI: `http://localhost:8090/docs`
- ReDoc: `http://localhost:8090/redoc`
- OpenAPI JSON: `http://localhost:8090/openapi.json`

<Callout type="info" title="CORS">
The REST server enables CORS by default for all origins in development. In production, configure allowed origins via `MNEMOSYNE_CORS_ORIGINS`.
</Callout>
