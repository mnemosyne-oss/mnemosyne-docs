# Quick Start

Get Mnemosyne v3.11.0 running in under 5 minutes. If you haven't installed yet, see the [Installation Guide](/getting-started/installation) first.

## Prerequisites

- Python 3.10 or higher
- Mnemosyne installed (`pipx install mnemosyne-memory[all]`)

## Your First Memories

<CodeBlock code={`from mnemosyne import Mnemosyne

mem = Mnemosyne()

# Store a memory with entity extraction
mem.remember(
    "Alice prefers dark mode for the UI. She's the project lead.",
    extract_entities=True,
    importance=0.8,
)

# Store with LLM fact extraction
mem.remember(
    "The deadline was moved to June 15th.",
    extract=True,
)

# Temporal recall — find what happened recently
results = mem.recall("UI preferences", top_k=5)

for r in results:
    print(f"[{r['score']:.3f}] {r['content']}")
`} language="python" />

## Configurable Retrieval

Mnemosyne v3 lets you tune scoring weights per query:

<CodeBlock code={`# Boost text matching over semantic similarity
results = mem.recall(
    "exact error code E501",
    vec_weight=20.0,
    fts_weight=60.0,
    importance_weight=20.0,
)

# Emphasize very recent memories
results = mem.recall(
    "what did we discuss today?",
    temporal_weight=0.6,
    temporal_halflife=24.0,  # 24-hour half-life
)
`} language="python" />

## Memory Banks

Isolate memories for different projects or contexts:

<CodeBlock code={`# Create a bank for work projects
work_mem = Mnemosyne(bank="work")
work_mem.remember("Deployed v3.11.0 to staging.")

# Separate bank for personal
personal_mem = Mnemosyne(bank="personal")
personal_mem.remember("Dentist appointment on Friday.")
`} language="python" />

## What's Next?

- Read the [Installation Guide](/getting-started/installation) for setup options, extras, and Hermes integration
- Explore the [Python SDK Reference](/api/python-sdk) for the full API
- Learn about the [BEAM Architecture](/architecture/beam-overview) to understand how memory works
- See [Hybrid Search](/retrieval/hybrid-search) for retrieval tuning details
